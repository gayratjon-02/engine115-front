export enum SUBSCRIPTION_PLAN {
    FREE = 'free',
    PRO = 'pro',
    AGENCY = 'agency',
}

export enum PLAN_STATUS {
    ACTIVE = 'active',
    CANCELLED = 'cancelled',
    PAST_DUE = 'past_due',
}
